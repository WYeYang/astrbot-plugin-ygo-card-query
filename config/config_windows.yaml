# 游戏王查卡插件配置文件 (# 游戏王查卡插件配置文件 (Windows)

# 数据库配置
d# 游戏王查卡插件配置文件 (Windows)

# 数据库配置
database:
  # 数据库目录路径
  db_dir: "ygopro_database"
  # 数据库更新源
  git_repo: "https://github.com/moecube/ygopro-database.git"
# 游戏王查卡插件配置文件 (Windows)

# 数据库配置
database:
  # 数据库目录路径
  db_dir: "ygopro_database"
  # 数据库更新源
  git_repo: "https://github.com/moecube/ygopro-database.git"
  # 数据库文件路径（相对于db_dir）
  cdb_path: "locales/zh-CN/cards.cdb"
  # 备用数据库文件路径
# 游戏王查卡插件配置文件 (Windows)

# 数据库配置
database:
  # 数据库目录路径
  db_dir: "ygopro_database"
  # 数据库更新源
  git_repo: "https://github.com/moecube/ygopro-database.git"
  # 数据库文件路径（相对于db_dir）
  cdb_path: "locales/zh-CN/cards.cdb"
  # 备用数据库文件路径
  fallback_cdb_path: "cards.cdb"

# 卡片查询配置
card_query# 游戏王查卡插件配置文件 (Windows)

# 数据库配置
database:
  # 数据库目录路径
  db_dir: "ygopro_database"
  # 数据库更新源
  git_repo: "https://github.com/moecube/ygopro-database.git"
  # 数据库文件路径（相对于db_dir）
  cdb_path: "locales/zh-CN/cards.cdb"
  # 备用数据库文件路径
  fallback_cdb_path: "cards.cdb"

# 卡片查询配置
card_query:
  # 默认查询语句
  default_sql: "SELECT d.id, t.name,# 游戏王查卡插件配置文件 (Windows)

# 数据库配置
database:
  # 数据库目录路径
  db_dir: "ygopro_database"
  # 数据库更新源
  git_repo: "https://github.com/moecube/ygopro-database.git"
  # 数据库文件路径（相对于db_dir）
  cdb_path: "locales/zh-CN/cards.cdb"
  # 备用数据库文件路径
  fallback_cdb_path: "cards.cdb"

# 卡片查询配置
card_query:
  # 默认查询语句
  default_sql: "SELECT d.id, t.name, d.type, d.attribute, d.level, d.race, d.atk, d.def